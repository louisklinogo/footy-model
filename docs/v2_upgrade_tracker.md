# V2 Upgrade Tracker

> Canonical for the active v2 challenger backlog.
> This is **not** the current live runtime source of truth; see `docs/current_state.md` for that.

Canonical strategy doc for the family replacement program: `docs/plans/2026-03-08-v2-production-standard-plan.md`.

Current implementation spec for Phase 1 scoreline work: `docs/plans/2026-03-08-scoreline-v21-implementation-spec.md`.

Updated: 2026-03-09 (anytime direct-monotone breakthrough + bundled challenger refresh)
Owner: Augment Agent
Status legend: TODO / IN_PROGRESS / DONE / BLOCKED

## Current baseline
- DONE: `pytest -q tests/v2`
- DONE: `pytest -q tests/test_sofascore_odds_parse.py tests/test_score_market_outcomes_corners.py tests/test_snapshot_leakage_guard.py`
- DONE: Live DB audit of coverage, dispersion, and odds-line support
- DONE: Live SofaScore path audit for stats, incidents, lineups, odds, and missing players

## Family replacement program baseline
- DONE: canonical strategy doc established at `docs/plans/2026-03-08-v2-production-standard-plan.md`
- DONE: Phase 1 implementation spec established at `docs/plans/2026-03-08-scoreline-v21-implementation-spec.md`
- DONE: reusable matched-cohort harness added at `src/modeling/v2/eval/live_replacement_compare.py`
- DONE: canonical baseline artifacts written to `artifacts/v2/family_replacement/live_replacement_20260306_phase1_baseline/`
- Latest benchmark truth is now `artifacts/v2/family_replacement/live_replacement_20260307_schema_aligned/summary.json`, not the earlier pre-fix reruns.
- Post-fix benchmark reread: the runtime binary-schema alignment fix materially strengthened the live benchmark path while leaving challenger outputs effectively unchanged. Matched fallback fell from `72.5%` to `19.9%` overall (`33,272 -> 9,132` rows), and `full_overlap_nonfallback` expanded from `12,644` to `36,784` rows.
- Learned conclusion: earlier broad challenger-positive claims were partly confounded by live runtime inference failures. The main benchmark flip is best understood as a corrected live baseline, not a challenger regression.
- Current matched verdict on the corrected benchmark: `full_overlap` now slightly favors live (`delta_auc -0.00149`, `delta_brier +0.00049`, `delta_log_loss +0.00099`), and `full_overlap_nonfallback` also slightly favors live.
- Scoreline governance update: scoreline is now split for scope/evaluation/promotion into `scoreline_directional`, `scoreline_totals_core`, `scoreline_multigoals`, and `scoreline_multiscore` while still sharing one underlying scoreline artifact/trainer.
- Scoreline promotion/baseline hardening update: `run_evaluation_flow.py` now preflight-validates baseline coverage for scoped and required markets before trusting promotion output, after a stale baseline registry briefly created misleading `missing_baseline` failures on canonical EH3 draw markets.
- Current passing scoreline artifact: `model_artifacts/v2/scoreline_v21_total_intensity_snap_20260308/` with evaluation `model_artifacts/v2/evaluation_scoreline_v21_total_intensity_snap_20260308_eps/`.
- Current pinned named baseline snapshot for future comparisons: `model_artifacts/v2/baselines/metrics_baseline_v2_scoreline_v21_total_intensity_snap_20260308_eps.json`.
- Self-check using the pinned named baseline also passes cleanly: `artifacts/tmp/evaluation_scoreline_v21_total_intensity_snap_selfcheck_named_baseline/` records `22/22` required markets passed and `76/76` scoped markets passed.
- `scoreline_directional` is now slightly positive on pooled overlap after the fix (`delta_auc +0.00014`, `delta_brier -0.00024`, `delta_log_loss -0.00050`), with a clearer true non-fallback edge (`delta_auc +0.00181`, `delta_brier -0.00103`, `delta_log_loss -0.00231`). Remaining fallback is concentrated in unsupported `ah_*` / `eh_*` markets; among true non-fallback directional markets, `1x2_a`, `1x2_d`, `dc_12`, and `dc_1x` lead, while `1x2_h` and `dc_x2` still slightly trail live.
- `scoreline_totals_core` remains blocked by `o15`: `u35` is mildly positive, but `o15` is materially negative, leaving the pooled two-market slice negative overall.
- `scoreline_multigoals` and `scoreline_multiscore` still have no matched live-overlap basis in this cohort, so there is no promotion evidence yet.
- Corners is no longer replacement-ready on the corrected benchmark. The pre-fix positive result was heavily confounded by live fallback (`66.7%` fallback pre-fix, `0.0%` post-fix). On the corrected overlap, corners turns negative (`delta_auc -0.00446`, `delta_brier +0.00116`, `delta_log_loss +0.00265`); only `c105` and `c95` are clearly positive across the main metrics, while most `ac_*` / `hc_*` markets and `c85` trail live. This weakness is broad rather than single-league noise.
- Anytime bounded rerun update (2026-03-08): replayed `anytime_rich_snapshot_v1` and a simple home-alpha retune under the current PIT dataset / pinned baseline. The alpha retune was discarded quickly. The rich snapshot candidate (`model_artifacts/v2/anytime_rich_snapshot_v1_candidate_20260308/`) is the only anytime path with real signal: versus the previous anytime challenger baseline it improves mean AUC by `+0.00237`, mean Brier by `-0.00080`, and mean log-loss by `-0.00227` across the four anytime markets.
- Live-overlap result for the rich anytime candidate: `artifacts/v2/family_replacement/live_replacement_20260308_anytime_rich_snapshot_v1/` turns the family from slightly negative to slightly positive on the non-fallback overlap (`delta_auc +0.00056`, `delta_brier -0.00013`, `delta_log_loss -0.00195`). `a_1up` and `a_2up` stay positive, `h_2up` improves materially, and `h_1up` remains the main residual weak point.
- Follow-up `h_1up` pass (2026-03-08): a `phase_split` challenger (`model_artifacts/v2/anytime_h1up_phase_split_candidate_20260308/`) improved promotion-holdout `h_1up`/`a_1up`, but materially worsened the live-overlap home ladders. That path is recorded as a useful no-go, not the next reference.
- Better bounded result: a contract-only challenger (`model_v2/feature_contracts/experiments/anytime_h1up_rich_snapshot_v1.yaml`) added a very small home-start-fast / lead-rate unblock on top of the rich snapshot artifact. The resulting artifact `model_artifacts/v2/anytime_h1up_rich_snapshot_v1_candidate_20260308/` improves all four anytime holdout markets versus `anytime_rich_snapshot_v1_candidate_20260308`, including `h_1up` (`ΔAUC +0.00086`, `ΔBrier -0.00024`, `Δlog-loss -0.00057`) while also improving the other three ladders.
- Live-overlap read for the contract-only challenger: `artifacts/v2/family_replacement/live_replacement_20260308_anytime_h1up_rich_snapshot_v1/` improves all four anytime per-market live deltas versus the prior rich challenger. Mean anytime live deltas move from `ΔAUC +0.00666`, `ΔBrier -0.00013`, `Δlog-loss -0.00195` to `ΔAUC +0.00741`, `ΔBrier -0.00029`, `Δlog-loss -0.00234`. `h_1up` and `h_2up` still trail live slightly, but both are less negative than before.
- Practical sequencing decision: `anytime_h1up_rich_snapshot_v1_candidate_20260308` is now the current leading anytime challenger reference, but bounded tuning is finished. The next anytime workstream is a structural family redesign, not more patching: see `docs/plans/2026-03-08-anytime-v2-state-ladder-recommendation.md` and `docs/plans/2026-03-08-anytime-v2-lead-process-implementation-spec.md`. Corners stays deferred to a later structural sprint focused on totals-first scope and dispersion/model-shape questions rather than more small feature-block tweaks.
- First structural redesign benchmark (2026-03-08): implemented `path_version=state_ladder` as the first anytime lead-process family mode, trained `model_artifacts/v2/anytime_state_ladder_v1_candidate_20260308/`, and benchmarked it via `model_artifacts/v2/evaluation_anytime_state_ladder_v1_candidate_20260308/` plus `artifacts/v2/family_replacement/live_replacement_20260308_anytime_state_ladder_v1/`. Result: useful no-go iteration, not the new anytime leader. It improved holdout AUC on `h_1up` / `h_2up` versus the bounded leader, but holdout Brier/log-loss worsened on the home ladders and live-overlap turned negative overall (`ΔAUC -0.00266`, `ΔBrier +0.00095`, `Δlog-loss +0.00120`). Per-market live deltas worsened versus the current anytime leader on all four ladders, especially `h_2up`. Therefore `anytime_h1up_rich_snapshot_v1_candidate_20260308` remains the leading anytime challenger reference while the structural redesign stays in active iteration.
- Second structural redesign pass (2026-03-08): upgraded `state_ladder` with chrono OOF priors plus internal head calibration and prior blending, then trained `model_artifacts/v2/anytime_state_ladder_v2_candidate_20260308/`. This fixed the worst v1 optimism, improved `h_1up` and `a_1up` holdout quality materially, and passed holdout versus baseline on both 1-up ladders, but it still failed on `h_2up` / `a_2up` proper scoring and remained worse than the current anytime leader on the matched live-overlap benchmark (`artifacts/v2/family_replacement/live_replacement_20260308_anytime_state_ladder_v2/`: anytime overlap `ΔAUC -0.00177`, `ΔBrier +0.00026`, `Δlog-loss -0.00062`).
- Third structural redesign pass (2026-03-08): fixed the remaining blend leak by fitting ladder calibration/blend from OOF head predictions and trained `model_artifacts/v2/anytime_state_ladder_v3_candidate_20260308/` with much stronger shrinkage (`home_1up=0.1`, `away_1up=0.1`, `home_2up_cond=0.0`, `away_2up_cond=0.1`). Holdout improved further for `h_2up` and `a_2up`, but live-overlap deteriorated again (`artifacts/v2/family_replacement/live_replacement_20260308_anytime_state_ladder_v3/`: anytime overlap `ΔAUC -0.00223`, `ΔBrier +0.00105`, `Δlog-loss +0.00107`), with both home ladders materially worse than the current anytime leader. Current conclusion: the state-ladder redesign path is scientifically informative but still not live-winning; `anytime_h1up_rich_snapshot_v1_candidate_20260308` remains the leading anytime challenger reference.
- Fourth structural redesign pass (2026-03-08): added `state_ladder_prior_version=constant`, meaning the ladder heads were built around a constant-path backbone derived by summing phase-goal rates rather than using the phase-split prior directly. This also failed to transfer live (`artifacts/v2/family_replacement/live_replacement_20260308_anytime_state_ladder_v4/`: anytime overlap `ΔAUC -0.00235`, `ΔBrier +0.00113`, `Δlog-loss +0.00124`), again with both home ladders worse than the current anytime leader.
- Fifth structural redesign pass (2026-03-08): implemented `state_ladder_prior_version=constant_model`, which trains direct constant-goals backbone regressors inside `state_ladder` so the ladder correction layer sits on the same model class as the current leader instead of a phase-sum surrogate. This improved holdout on `h_1up` and `a_1up` and gave the strongest leader-like structural baseline attempted so far (`model_artifacts/v2/anytime_state_ladder_v5_candidate_20260308/`), but it still lost on matched live-overlap overall (`artifacts/v2/family_replacement/live_replacement_20260308_anytime_state_ladder_v5/`: anytime overlap `ΔAUC -0.00177`, `ΔBrier +0.00089`, `Δlog-loss +0.00068`). Per-market live deltas confirm the persistent failure mode: away ladders remain positive but weaker than the current leader, while home ladders worsen materially (`h_1up ΔAUC -0.00657`, `h_2up ΔAUC -0.01154`). Practical conclusion: the current `state_ladder` research branch has been pushed through five serious iterations and still does not beat the bounded anytime leader on live-overlap, so further anytime work should not continue as small ladder tweaks; it now needs a different modeling hypothesis focused explicitly on home-ladder transfer.
- Different model-class breakthrough (2026-03-09): implemented `path_version=direct_monotone`, a stacked anytime family that keeps the current leader-style constant-goals backbone, adds direct binary heads for `h_1up`, `a_1up`, `h_2up`, `a_2up`, calibrates/blends them using chrono OOF predictions, and enforces `2up <= 1up` in derivation. The first candidate `model_artifacts/v2/anytime_direct_monotone_v1_candidate_20260308/` is the first anytime branch to beat the current anytime leader on the matched live-overlap benchmark: `artifacts/v2/family_replacement/live_replacement_20260308_anytime_direct_monotone_v1/` improves aggregate anytime overlap from `ΔAUC +0.00107`, `ΔBrier -0.00029`, `Δlog-loss -0.00234` to `ΔAUC +0.00149`, `ΔBrier -0.00041`, `Δlog-loss -0.00261`. Per-market live changes versus the prior leader: `a_1up` improves, `h_1up` improves, and `a_2up` / `h_2up` remain unchanged. Holdout also improves on `h_1up` and `a_1up` while preserving `h_2up` / `a_2up`. Practical conclusion: `anytime_direct_monotone_v1_candidate_20260308` is now the current leading anytime challenger reference.
- Bundled challenger refresh (2026-03-09): reran the full bundle with `scoreline_v21_total_intensity_snap_20260308`, the current resolved corners reference (`model_artifacts/v2/live_verification_corners_20260306/`), and `anytime_direct_monotone_v1_candidate_20260308`. The refreshed bundle evaluation `model_artifacts/v2/evaluation_bundle_refresh_anytime_direct_monotone_v1_20260309/` passes the required policy gate (`22/22` required markets passed, `75/76` scoped markets passed). The refreshed live compare `artifacts/v2/family_replacement/live_replacement_20260309_bundle_anytime_direct_monotone_v1/` modestly improves full-bundle overlap versus the prior anytime-led bundle (`ΔAUC +0.000559 -> +0.000574`, `ΔBrier -0.000327 -> -0.000337`, `Δlog-loss -0.000844 -> -0.000868`). This gain comes entirely from the anytime slice (`a_1up` and `h_1up` improve; `a_2up` / `h_2up` unchanged), while scoreline and corners are unchanged. Practical conclusion: this is the current repo-backed bundled challenger reference.
- Corners totals-first structural sprint (2026-03-10): replayed the corners family as a deeper structural program around a totals-first backbone. The sprint trained and benchmarked `totals_first_v1`, `totals_first_residual_v1`, `totals_first_market_heads_v1`, `signal_expanded_v1`, `signal_expanded_noodds_v1`, `signal_expanded_league_style_v1`, `team_market_calibrated_league_style_v1`, `totals_surface_calibrated_league_style_v1`, `totals_first_league_share_residual_v1`, and `pmf_surface_blended_v1`. None promoted; the best matched live-overlap result stayed `signal_expanded_league_style_v1` at corners overlap `ΔAUC -0.003596`, `ΔBrier +0.000492`, `Δlog-loss +0.001019`, with only `1/12` corners markets passing evaluation.
- Corners signal-limit review (2026-03-10): the strongest gain in the sprint came from feature-side expansion, not from later architecture changes. After the feature-expanded leader, every architecture-first follow-up regressed on matched live-overlap. The current PIT audit snapshot also shows extremely sparse totals odds support (`3.647%` rows with any complete `c75/c85/c95/c105` over/under pair, `0.00%` with all four pairs complete) and no team-corners odds columns. A contract-safe direct-classifier sanity probe on the active corners feature contract reached only mean AUC `0.586761`, mean Brier `0.218371`, and mean log-loss `0.627945`, while unconstrained raw-table sweeps immediately leak through target-derived corners columns. Practical conclusion: corners is now best understood as signal/support-limited under the valid pre-match PIT setup; future corners work should require new signal/support, not more architecture-only reshuffles. See `docs/plans/2026-03-10-corners-signal-limit-review.md`.
- Corners new-signal acquisition plan (2026-03-10): wrote the concrete roadmap to `docs/plans/2026-03-10-corners-new-signal-acquisition-plan.md`. Immediate priority is an experimental contract pass adding existing PIT-safe `xgot/xa`, `big_chances`, and broadened `crosses` blocks, because direct chrono-safe probes improved the current 41-feature corners base from mean Brier `0.218371` to `0.217674` with `xgot/xa + big_chances`. Medium-term priorities are surfacing `rolling_tackles_pct` / `rolling_errors_lead_to_shot` from `team_premium_snapshots`, repairing style-cluster coverage before PIT integration, and treating availability / team-corners odds as operational unblockers rather than immediate modeling inputs.
- Corners Phase A execution result (2026-03-10): completed the contract-only signal pass. The full bundle `model_v2/feature_contracts/experiments/corners_signal_phasea_v1.yaml` -> `model_artifacts/v2/corners_signal_phasea_v1_candidate_20260310/` lost clearly on matched live corners overlap (`ΔAUC -0.008589`, `ΔBrier +0.002084`, `Δlog-loss +0.004831`). A bounded salvage variant without crosses (`corners_signal_phasea_core_v1_candidate_20260310/`) improved that result but still stayed behind the current leader (`ΔAUC -0.005568`, `ΔBrier +0.001119`, `Δlog-loss +0.002465` vs leader `-0.003596 / +0.000492 / +0.001019`). Practical conclusion: Tier 1 contract-only additions are informative but insufficient; if corners work continues, move to Phase B PIT extraction of defensive-pressure signals rather than more small contract reshuffles.
- Corners Phase B execution result (2026-03-10): completed the defensive-pressure extraction pass. Added `rolling_errors_lead_to_shot` and `rolling_tackles_pct` (plus against variants and missingness indicators) through both `market_outcome_calibrator.fetch_dataset(...)` and `predict_market_outcomes_fixtures_first.fetch_candidate_fixtures(...)`, rebuilt a PIT snapshot, and benchmarked `model_artifacts/v2/corners_signal_phaseb_pressure_v1_candidate_20260310/`. Coverage on the rebuilt PIT snapshot was real (`rolling_errors_lead_to_shot*` missing ~`32.1%`, `rolling_tackles_pct*` missing ~`14.5-14.6%`), but matched live corners overlap regressed sharply to `ΔAUC -0.017072`, `ΔBrier +0.004888`, `Δlog-loss +0.012008`, materially worse than the current leader. Practical conclusion: the strongest currently justified corners extraction seam has been tested and failed; corners should now be parked / blocked until upstream support materially improves.
- Corners final bounded feature-engineering pass (2026-03-10): completed one last DB-audit-driven snapshot-quality experiment. Added `rolling_rest_days`, `fidelity_score`, and compact lead-rate/rest/fidelity derived features plus missingness indicators through both PIT and live paths, then benchmarked `model_artifacts/v2/corners_signal_final_snapshot_quality_v1_candidate_20260310/`. Mechanical validation passed (`30` focused tests, PIT rebuild, smoke train/predict), but matched live corners overlap worsened materially to `ΔAUC -0.01609`, `ΔBrier +0.00459`, `Δlog-loss +0.01161`, and all `12/12` corners markets failed evaluation. Practical conclusion: the last bounded repo-justified corners feature-engineering seam under the current data/support regime has now also failed.
- Corners bounded autoresearch audit (2026-03-10): ran a constrained repo/DB seam search instead of open-ended AutoML. It verified that `fixture_odds_markets` is already the active normalized corners odds path, old `fixture_odds_snapshots` has effectively zero usable corners coverage, and there are still no team-corners odds. The most credible remaining unsurfaced repo seam is situational context features from `src/features/situational_features_v2.py` (`season_pct_complete`, `rest_delta`, `position_gap`, `motivation_tier_*`, etc.), but quick signal checks suggest they are incremental context modifiers rather than the missing direct corners signal, and availability-driven subfeatures are not operationally populated for upcoming fixtures. Practical conclusion: no further corners cycle is justified right now; park the family until upstream support materially improves.
- Corners style-matchup possession-only follow-up (2026-03-12): resumed the March 11 style-cluster interaction branch and ran the recommended ablation on the same PIT snapshot (`artifacts/tmp/corners_possession_ablation_20260312T014826Z/`). Internally, the possession-only interaction variant beat the plain style-matchup reference on holdout (`AUC 0.60687 vs 0.60251`, `Brier 0.21366 vs 0.21454`), confirming that the cluster taxonomy helps mainly as `style_matchup x possession` context rather than as a flat label. However, the repo-backed challenger artifact `model_artifacts/v2/corners_totals_first_style_matchup_possession_only_featurepass_autoresearch_v1_candidate_20260312/` failed the normal bundle promotion run `model_artifacts/v2/evaluation_corners_totals_first_style_matchup_possession_only_featurepass_autoresearch_v1_candidate_20260312/`: only `c75` passed among corners, while `c85`, `c95`, `c105`, and all `hc*/ac*` markets failed versus the pinned baseline. Practical conclusion: useful research confirmation, not a new promoted corners reference.
- Corners style-matchup possession-plus-box-touches follow-up (2026-03-12): built a bounded interaction autoresearch loop, searched single-block and focused combo challengers under `artifacts/tmp/corners_interaction_search_batch{1,2,3}_20260312/`, and converged on `style_matchup_possession + style_matchup_box_touches` as the strongest corners branch. Materialized repo-backed candidate: `model_artifacts/v2/corners_totals_first_style_matchup_possession_box_touches_featurepass_autoresearch_v1_candidate_20260312/`. Formal evaluation output: `model_artifacts/v2/evaluation_corners_totals_first_style_matchup_possession_box_touches_featurepass_autoresearch_v1_candidate_20260312/`. Result improved on the earlier possession-only candidate but still failed full-scope promotion: `66 / 76` markets passed overall versus `64 / 76` before, with corners `c75`, `c85`, and `hc25` now passing, but `c95`, `c105`, `hc35`, `hc45`, `hc55`, and all `ac*` markets still failing against the pinned baseline. Practical conclusion: hidden interaction signal is real and box touches is the best companion found so far, but the current corners incumbent remains ahead on the remaining upper-ladder and away-team markets.
- Corners team-market-calibrated away-share follow-up (2026-03-12): tested the most direct `ac*` repair path by keeping the winning `style_matchup + possession + box_touches` feature surface but switching the corners path to `totals_first_team_market_calibrated`. Repo-backed challenger: `model_artifacts/v2/corners_totals_first_style_matchup_possession_box_touches_team_market_calibrated_featurepass_autoresearch_v1_candidate_20260312/`. Formal evaluation output: `model_artifacts/v2/evaluation_corners_totals_first_style_matchup_possession_box_touches_team_market_calibrated_featurepass_autoresearch_v1_candidate_20260312/`. Result was worse than the non-head version: `65 / 76` markets passed overall, all `ac*` markets still failed, and the branch also regressed `hc25/hc35/hc45/hc55`, leaving only `c75` and `c85` among corners markets as passes. Practical conclusion: direct calibrated team heads are not the right next fix for this style-conditioned branch; the away-team weakness is more likely a ranking/tail representation issue than something solved by swapping in team-market heads.
- Corners away-tail interaction follow-up (2026-03-12): added new PIT-safe interaction families for `style_matchup x corners_against` and `style_matchup x sot_against` in `src/modeling/v2/families/corners/features.py`, exposed them through the bounded search library, and fixed the bounded runner so focused searches can carry `required_block_names` instead of silently dropping anchor blocks from managed feature contracts. The corrected anchored batch under `autoresearch_corners/results/corners_interaction_search_batch5_away_tail_anchored_20260312_20260312T102918Z/` found a small winner: `style_matchup_possession + style_matchup_box_touches + style_matchup_corners_against` beat the prior anchored branch on live overlap (`ΔAUC -0.00125` vs `-0.00148`, `ΔBrier -0.000254` vs `-0.000169`). Materialized repo-backed challenger: `model_artifacts/v2/corners_totals_first_style_matchup_possession_box_touches_corners_against_featurepass_autoresearch_v1_candidate_20260312/`. Formal evaluation output: `model_artifacts/v2/evaluation_corners_totals_first_style_matchup_possession_box_touches_corners_against_featurepass_autoresearch_v1_candidate_20260312/`. Result improved the branch again to `67 / 76` passed markets overall, newly passing `ac45`, but still failed full-scope promotion on `c95`, `c105`, `hc35`, `hc45`, `hc55`, `ac25`, `ac35`, `ac55`, and `a_1up`. Practical conclusion: concession-style corners interactions help, but only marginally; the branch remains non-promotable and the unresolved weakness is now concentrated in upper-ladder totals plus the lower/upper away-team corner thresholds.
- Corners total-tail interaction follow-up (2026-03-12): extended the PIT-safe style-matchup interaction surface with `box_touches_against`, `crosses`, and `crosses_against`, then ran a focused anchored totals-tail search under `autoresearch_corners/results/corners_interaction_search_batch6_total_tail_anchored_20260312_20260312T141425Z/` using the current best backbone (`style_matchup_possession + style_matchup_box_touches + style_matchup_corners_against`) as required blocks. None of the new total-tail additions helped. The anchor-only baseline remained best in the batch; `style_matchup_box_touches_against` was slightly less bad than the other add-ons but still worse than the current backbone, while both `crosses` and `crosses_against` variants materially regressed live overlap and holdout quality. Practical conclusion: the current `c95/c105` weakness is not fixed by simply adding more style-gated pressure/territory proxies on top of the totals-first branch.
- Corners path-level totals-tail follow-up (2026-03-12): reran the current best feature backbone (`style_matchup_possession + style_matchup_box_touches + style_matchup_corners_against`) through both explicit totals-tail paths now available in the trainer: `totals_surface_calibrated` and `totals_surface_neural_ladder_calibrated`. Repo-backed challengers: `model_artifacts/v2/corners_totals_surface_calibrated_style_matchup_possession_box_touches_corners_against_featurepass_autoresearch_v1_candidate_20260312/` and `model_artifacts/v2/corners_totals_surface_neural_ladder_style_matchup_possession_box_touches_corners_against_featurepass_autoresearch_v1_candidate_20260312/`. Formal evaluation outputs: the matching `evaluation_*` directories under `model_artifacts/v2/`. Both paths were worse than the current best totals-first challenger, landing at `65 / 76` passed markets and newly losing `c75` and `c85` while still failing `c95/c105`. Practical conclusion: the remaining corners tail weakness is not fixed by either the calibrated totals-surface path or the neural ladder path on the current feature backbone.

## Execution board
1. [DONE] Build evaluation and promotion harness
   - Add/finish v2 eval runner
   - Add/finish v2 metrics helpers
   - Add/finish promotion registry with explicit reason codes
   - Acceptance: challenger vs champion comparisons are explicit and reproducible

2. [DONE] Add reversible candidate artifact/version plumbing
   - Separate output dirs for challengers
   - Distinct `model_version` values
   - Acceptance: challenger runs cannot overwrite champion artifacts

3. [DONE] Create corners live-scope candidate
   - Candidate scope centered on `c85`, `c95`, `c105`
   - `c75` non-critical until live support improves
   - Team-corners remain research-only
   - Acceptance: production-critical corners align with actual odds support
   - Result: `model_v2/market_scope_corners_live_candidate.yaml` and isolated `corners_live_scope_candidate_v1` artifacts created

4. [DONE] Benchmark corners challengers
   - Champion vs live-scope candidate
   - Global dispersion vs league/shrinkage-aware dispersion
   - Acceptance: improvement on promotion-critical markets without stability regressions
   - Current result: live-scope candidate matched champion metrics on `c85`, `c95`, `c105` (no uplift yet)
   - Calibration rerun result: `c105` improved on the calibration eval split under sigmoid, but promotion correctly stays on raw holdout because calibrated metrics were computed on a smaller eval-only subset (`n=764` vs raw holdout `n=1527`)
   - Fresh bounded rerun result (2026-03-08): `corners_attack_block_candidate_20260308` was trained and evaluated with the current PIT dataset / harness against pinned baseline `metrics_baseline_v2_scoreline_v21_total_intensity_snap_20260308_eps.json`. It improved some team-corners markets (`hc25`, `hc35`, `ac25`, `ac35`) but **worsened all four totals** (`c75`, `c85`, `c95`, `c105`) on AUC, Brier, and log-loss versus the current corners baseline. Evaluation artifact: `model_artifacts/v2/evaluation_corners_attack_block_candidate_20260308/`.
   - Practical conclusion: stop the attack-block path here. Team-corners remain research-only until market support improves, and a candidate that loses on the totals surface is not the right next production challenger. Revisit corners later as a dedicated structural sprint, not as another small bounded feature tweak.
   - Totals-first structural sprint result (2026-03-10): the dedicated structural rerun is now complete and points to the same practical answer. `signal_expanded_league_style_v1` is the best corners challenger from the sprint, but it is still non-promotable and still slightly worse than live on matched overlap (`ΔBrier +0.000492`). All deeper architectural variants after that feature-expanded leader lost additional ground.
   - Practical conclusion update: corners should not continue as architecture-only iteration under the current PIT feature/support regime. The next justified corners pass requires genuinely new signal/support, not more internal reshuffling of the current model family.
   - Concrete follow-up plan (2026-03-10): next corners work should start with a contract-only signal acquisition pass (`xgot/xa`, `big_chances`, broadened `crosses`), then move to PIT extraction of defensive pressure fields (`rolling_tackles_pct`, `rolling_errors_lead_to_shot`), then style-cluster productionization. Availability / lineup features and team-corners odds remain blocked by current snapshot sparsity.
   - Final closeout update (2026-03-10): the bounded defensive-pressure pass, the bounded snapshot-quality pass, and a final bounded autoresearch seam audit have all now been completed. None changed the practical verdict. Corners is closed for now and should only reopen if upstream support materially changes (team-corners odds, denser totals odds, stronger style coverage, or genuinely populated near-kickoff availability).

5. [DONE] Make calibration a first-class v2 stage
   - Added row-level holdout prediction artifacts across families
   - Added aggregated calibration runner and family-level calibration artifacts
   - Promotion can read calibration diagnostics
   - Guard added: only prefer calibrated holdout metrics for promotion when the calibrated sample is comparable to the raw holdout sample
   - Acceptance: calibration exists in the real evaluation flow and does not create apples-to-oranges promotion comparisons

6. [DONE] Clean the scoreline PIT path and benchmark the first real PIT challenger
   - Added PIT safety guard so scoreline training refuses dataset artifacts whose sibling `pit_validation_report.json` is failed
   - Fixed the odds snapshot seam first, then found and fixed the remaining uncapped lambda-predictions seam in `market_outcome_calibrator.fetch_dataset()`
   - Real result: `scoreline_pit_candidate_v2` now passes promotion on 59 / 68 scoped markets and all core scoreline markets (`1x2_*`, `dc_*`, `ah_*`, `eh_*`, `o15`, `u35`)
   - Remaining failures are concentrated in 9 tail scoreline bucket markets (`mg_*`, `hmg_*`, `ms_other_awaywin`) and currently fail on AUC only while Brier / log-loss / ECE improved

7. [DONE] Add PIT-safe availability features
   - Missing-player counts / lineup-known flags / freshness
   - Acceptance: leakage-safe feature generation and stable fallback behavior
   - Implementation update: added shared availability SQL helpers in `src/modeling/availability_features.py` and wired them into both PIT training extraction (`market_outcome_calibrator.fetch_dataset()`) and fixtures-first prematch scoring (`predict_market_outcomes_fixtures_first.py`).
   - Leakage/fallback update: availability snapshots are now capped at the PIT prediction cutoff, scoreline contracts include the new availability fields plus missingness indicators, and PIT validation now explicitly fails if availability or lineup timestamps appear after `prediction_time_utc`.
   - Verification: `pytest -q tests/v2/test_availability_features.py tests/v2/test_pit_leakage_guard.py tests/v2/test_shadow_compare.py tests/v2/test_evaluation_flow_runner.py tests/v2/test_scoreline_train_utils.py` → 28 passed.
   - Live rerun update: built `artifacts/v2/datasets/scoreline_availability_candidate_v1/pit_dataset_20260306T132736Z.csv` and trained `model_artifacts/v2/scoreline_availability_candidate_v1` from it. PIT leakage validation passed on 8,170 rows with zero violations.
   - Coverage note: prematch availability coverage in this PIT slice is still very sparse (60 rows with non-null home/away availability freshness; ~0.73% availability-known rate for each side), so the feature family is now safely available but not yet broadly informative across the historical sample.
   - Operational audit closeout: historical `player_availability` coverage is dense ex post (~96.6% of real fixtures eventually have rows), but true observed pre-kickoff capture is only ~1.0% of fixture sides overall and ~0.76% by `T-6h`, so this is not a meaningful signal for the current single-pass prematch horizon.
   - Source comparison closeout: Flashscore lineups pages do show pre-kickoff `Predicted lineups` for some larger fixtures, but the current legacy parser does not extract them and the sample did not show reliable structured missing-player sections. Decision: do not spend more modeling/scraper effort here unless product scope changes to include a near-kickoff refresh (`T-90m`/`T-60m`).

8. [DONE] Shadow-mode compare and decide promotion
   - Side-by-side predictions under distinct versions
   - Explicit registry verdict
   - Acceptance: rollout decision is reversible and evidence-based
   - Current recommendation: advance `scoreline_pit_candidate_v2` as the new leading scoreline candidate for core markets, while keeping the 9 tail scoreline bucket markets on a follow-up optimization track
   - Tail-market audit update: the remaining failed scoreline markets split into (a) overlapping range buckets (`mg_*`, `hmg_*`) and (b) one ultra-rare residual bucket (`ms_other_awaywin`, ~10 holdout positives). All 9 currently fail on AUC only under zero AUC tolerance, while Brier / log-loss / ECE improved.
   - Practical implication: these 9 markets deserve different attention from the core scoreline markets and should not be treated as equal blockers unless business policy explicitly says every bucket market is promotion-critical.
   - Implementation update: `src/modeling/v2/eval/promotion_registry.py` and `src/modeling/v2/run_evaluation_flow.py` now support optional repeated `--required-market` arguments. Default behavior is unchanged, but promotion reports can now emit a top-level decision based on an explicit core-market list while still preserving per-market pass/fail across the full scope.
   - Concrete core-market verdict: reran the registry for `scoreline_pit_candidate_v2` with required core markets (`1x2_*`, `dc_*`, `ah_h05`, `ah_a05`, `ah_h15`, `ah_a15`, `eh_h1`, `eh_a1`, `o15`, `u35`) and saved `model_artifacts/v2/evaluation_scoreline_pit_candidate_v2/promotion_registry_core_markets.json`. Result: top-level decision `passed`, with 14 / 14 required markets passed and 0 missing.
   - Operationalization update: added repo-backed promotion policy file `model_v2/promotion_policies/scoreline_core.yaml` plus optional `--promotion-policy` support in both evaluation CLIs, so future scoreline promotion runs can use a stable tracked core-market contract instead of repeating long `--required-market` lists.
   - Policy-backed rerun result: `model_artifacts/v2/evaluation_scoreline_pit_candidate_v2/promotion_registry_scoreline_core_policy.json` now records the same verdict through the repo policy file path itself (`scoreline_core_v1`), with decision `passed`, 14 / 14 required markets passed, and 0 missing required markets.
   - Decision artifact update: `src/modeling/v2/run_evaluation_flow.py` now reads the generated `promotion_registry.json` back into `evaluation_flow_report.json` as a compact `promotion_summary` block, so shadow/promotion verdicts are visible directly from the main runner report instead of only inside the full registry payload.
   - End-to-end flow rerun: `model_artifacts/v2/evaluation_scoreline_pit_candidate_v2_policyflow/evaluation_flow_report.json` now contains the compact `promotion_summary` block for the repo-backed policy path, again showing decision `passed`, 59 / 68 markets passed overall, and 14 / 14 required scoreline core markets passed.
   - Recommendation artifact update: added `src/modeling/v2/eval/promotion_recommendation.py` and wired `run_evaluation_flow.py` to emit `promotion_recommendation.json` and `promotion_recommendation.md`. The recommendation layer is deliberately derived-only: it turns the policy-backed decision into an explicit recommendation (`full_scope`, `required_markets_only`, or hold) plus a checklist and follow-up market list.
   - Real recommendation artifacts: `model_artifacts/v2/evaluation_scoreline_pit_candidate_v2_policyflow/promotion_recommendation.json` and `.md` now recommend `required_markets_only` promotion for the PIT scoreline candidate, with 14 / 14 core markets passed and follow-up tail markets explicitly listed (`mg_1_5`, `mg_2_4`, `mg_2_5`, `mg_2_6`, `hmg_0`, `hmg_1_2`, `hmg_1_3`, `hmg_2_3`, `ms_other_awaywin`).
   - Shadow compare artifact update: added `src/modeling/v2/eval/shadow_compare.py` and wired `src/modeling/v2/run_evaluation_flow.py` to emit derived `shadow_comparison.json` and `shadow_comparison.md` artifacts that pair challenger/champion versions per family and include per-market metric deltas from the promotion registry.
   - Practical result: future evaluation-flow reruns now automatically leave behind a concise baseline-vs-challenger comparison artifact alongside the promotion recommendation, without changing the existing promotion decision semantics.
   - Live availability challenger rerun: evaluated `model_artifacts/v2/scoreline_availability_candidate_v1` via `model_artifacts/v2/evaluation_scoreline_availability_candidate_v1_policyflow`. Result stayed policy-positive: top-level decision `passed`, 59 / 68 markets passed overall, and 14 / 14 required scoreline core markets passed.
   - Live shadow result: `shadow_comparison.md` now shows the availability challenger against baseline `poisson_head_v1`, with all 14 required/core markets improving on the tracked deltas (positive ΔAUC and lower Brier / log-loss / ECE in the emitted comparison table).
   - Artifact polish fix: after the first live rerun exposed misleading shadow-report fallback text, updated `shadow_compare.py` so the report now carries the real recommendation scope (`required_markets_only`) and family-default version labels for unchanged corners/anytime artifacts.

## Required tests as work lands
- `pytest -q tests/v2`
- `pytest -q tests/test_sofascore_odds_parse.py tests/test_score_market_outcomes_corners.py tests/test_snapshot_leakage_guard.py`
- Add targeted tests for every new seam before broadening scope
- Prefer smallest-scope train/eval smoke runs before full evaluation

## Promotion checklist
- No PIT leakage failures
- No unit/integration regressions
- Better or equal metrics on promotion-critical markets
- Calibration not materially worse
- Adequate support
- Gains stable across folds and leagues
- Champion artifacts still intact

## Notes to future self
- Do not assume Bash is the default shell even if available.
- Prefer temp Python scripts for DB probes and structured diagnostics.
- Do not promote team-corners just because model math improves; wait for market support.
- Do not claim scoreline dependence improvements without slice-level evidence.
- Do not treat pre-fix live-replacement benchmarks as current truth after a runtime fallback/inference-path fix; re-read the corrected artifacts first.
- When a benchmark flips after a runtime fix, check whether the challenger changed or whether the live/champion path simply stopped failing at inference time.
- Do not substitute calibrated holdout metrics into promotion unless the calibrated sample matches the raw holdout comparison set.
- Do not label a challenger as PIT-safe while the dataset artifact fails its own PIT validation report.
- Do not let secondary overlapping scoreline bucket markets block a clearly better core scoreline challenger without an explicit business reason.
- Do not trust a generic frozen baseline registry blindly; validate that required/scope markets are present before interpreting promotion failures, especially after contract/scope changes.
- When a scoreline artifact clears the gate, pin a named baseline snapshot for future comparisons instead of relying only on the mutable generic `metrics_baseline_v2.json` file.
